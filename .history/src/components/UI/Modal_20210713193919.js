import classes from './Modal.module.css';

const Backdrop = props => {
    return(<div className={classes.}></div>);
};
const Overlays = props => {return(<div className={classes}></div>);};

const Modal = props => {
    return ();
};
export default Modal;