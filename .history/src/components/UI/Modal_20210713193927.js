import classes from './Modal.module.css';

const Backdrop = props => {
    return(<div className={classes.Backdrop}></div>);
};
const Overlays = props => {return(<div className={classes.Overlays}></div>);};

const Modal = props => {
    return ();
};
export default Modal;