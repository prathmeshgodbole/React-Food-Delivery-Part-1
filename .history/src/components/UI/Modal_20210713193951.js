import classes from './Modal.module.css';

const Backdrop = props => {
    return(<div className={classes.backdrop}></div>);
};
const Overlays = props => {return(<div className={classes.modal}></div>);};

const Modal = props => {
    return ();
};
export default Modal;