import classes from './Cart.module.css';

const Cart = (props) => {
    const CartItems=[{id:'c1', ]
    return ( <div>
        <div></div>
        <div></div>
    </div>
    );
};

export default Cart;